name = input("wass ur name nigga? ").strip().title()
#variable(name) store
#spliting
first,last = name.split(" ")
# {} makes the str special and more versatile instead of using more commands , f as prefix activates (formats) {}
print(f"wass up,{first}" )
#this is manupulation of strings
# now lets start int = integers